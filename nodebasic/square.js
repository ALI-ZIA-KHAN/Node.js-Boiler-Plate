// exports.area = (a)=>(a*a);
// exports.parameter = (a)=> (4*a);

const area = (a)=>(a*a);
const parameter = (a)=> (4*a);


module.exports.area=area;
module.exports.parameter=parameter; //it can be like module.experts.parameter=tcdgvhhasha;
                                    // first is your variable which should be same  
